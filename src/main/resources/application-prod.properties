spring.application.name=resume-craft-backend

# Production profile uses a persistent database; change URL/user/password for real environment
spring.datasource.url=jdbc:mysql://${DB_HOST:resume-craft-db.cbosy82gyx7h.ap-south-1.rds.amazonaws.com}:3306/${DB_NAME:resumecraft}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:root911911}


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.defer-datasource-initialization=false

spring.h2.console.enabled=false
server.port=8080

management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=health,info

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
